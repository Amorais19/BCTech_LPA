import styled from "styled-components";
import { colors } from '../../styles/GlobalStyle'; // ajuste o caminho conforme necessário

export const Main = styled.main`
  min-height: calc(100vh - 9rem);
  background-color: ${colors.black};
`;
